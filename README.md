# TALLER-3
Para ejecutar el proyecto se debe de abrir por visual code o Intellij, abrir la terminal dentro de la carpeta del proyecto donde está `server.js` ejecutar `npm install` para instalar y luego ejecutar `node server.js` en la terminal de proyecto, si todo está correcto, la terminal mostrará que el servidor corre en `http://localhost:3000`.
